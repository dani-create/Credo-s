CHANGES — Corrections récentes

2025-11-17 — Corrections principales

- Fix: Corrigé la syntaxe CSS dans `styles/sections.css` (propriétés orphelines). Consolidation dans `.hero-btn`.
- Fix: Ajout de `line-clamp` standard en plus de `-webkit-line-clamp` pour compatibilité.
- Fix: Ajout de media queries pour écrans 414px (iPhone XR) et 375px (iPhone 6). Ajustements pour `hero-image`, `dishes-smoke-bg`, `.dish-card`, `.delivery-logo`.
- Fix: Forcé le masquage du `order-modal` au chargement via `javascript/modal.js`.
- Fix: Réparé le bouton galerie flottant (`#gallery-btn`) pour ouvrir la galerie dans le modal général.
- Fix: Ajout de la classe `order-modal` aux `div` du modal commande dans `index.html` et `templates/base.html` pour assurer la fermeture via Escape et boutons close.

Notes:
- Il reste recommandé de tester sur appareils réels (iPhone XR/6) ou via un simulateur pour vérifier les micro-ajustements visuels.
- Si vous souhaitez que je commit & push ces changements, autorisez-moi à exécuter les commandes Git (je peux exécuter localement les commandes via le terminal si vous me demandez).
